apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ipc-node-validator.fullname" . }}-test-connection"
  labels:
    {{- include "ipc-node-validator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ipc-node-validator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
