apiVersion: v1
kind: Service
metadata:
  name: {{ include "ipc-node-validator.fullname" . }}
  labels:
    {{- include "ipc-node-validator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ipc-node-validator.selectorLabels" . | nindent 4 }}
